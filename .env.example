# Copy to ~/.config/notify/.env and set your real ntfy topic.
#   cp .env.example ~/.config/notify/.env
# The topic is any string; anyone who knows it can publish/subscribe,
# so use a long, hard-to-guess value.
NTFY_TOPIC=change-me-to-your-topic
